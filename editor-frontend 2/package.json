{
  "name": "editor-frontend",
  "version": "1.0.0",
  "description": "Frontend for the editor, compiled into static assets",
  "scripts": {
    "build": "mkdir -p build && cp src/index.html build && cp src/app.js build && cp src/config.json build",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {}
}
